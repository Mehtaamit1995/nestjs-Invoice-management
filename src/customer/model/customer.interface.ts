export interface Customer {

        name?: string,
        email?: string,
        phone?: number,
        address?: string,
        city?: string,
        state?: string,
        postal_code?: number
        
}